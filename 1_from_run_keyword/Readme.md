# FROM command

```
From command is used to add the base docker image for the new docker image.
Commonly used Docker images are **ubuntu** for linux and "python-3" for python.
```

# RUN command

```
RUN command is used to execute certain commands that are executable in base image.
It is basically used to add functionality in the images before their containers are created.
```

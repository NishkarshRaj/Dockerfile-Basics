# ENV command in Dockerfile
```
ENV command is used to create environment variables for the containers.
```

## Syntax:
```Dockerfile
ENV <variable>=<value> # Variable and value must not have any space between them
```
> Or
```Dockerfile
ENV <variable> <value>
```
